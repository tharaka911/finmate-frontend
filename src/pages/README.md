# Pages
This folder contains the "Smart" components that are connected to your **Routes**.

- Pages should generally be thin.
- They "assemble" components and features into a full screen.
- They handle the high-level layout of a specific URL.

## Example:
`Dashboard.jsx` is the page for `/`.
`Transactions.jsx` is the page for `/transactions`.
