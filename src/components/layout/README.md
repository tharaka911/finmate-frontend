# Layout Components
This folder contains structural pieces of the UI that are reused across different pages.

- `Header.jsx`: The top navigation bar.
- `Footer.jsx`: The bottom bar (if added later).
- `Sidebar.jsx`: Side navigation (if added later).

These are different from "Page Layouts" because they are small pieces of a page, not the whole page structure.
