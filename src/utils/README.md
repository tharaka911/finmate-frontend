# Utilities
This folder is for "Pure Functions" that are used throughout the app.

- They do **NOT** use React. 
- They should be easy to test.
- They help keep your components small by moving logic out.

## Examples:
- `currency.js`: For formatting numbers as money.
- `validation.js`: For checking if email/password inputs are valid.
- `date.js`: For formatting timestamps.
