# Constants
This folder is for static values that never change during the app's execution.

- Config values.
- Shared text strings.
- Fixed lists (like categories or transaction types).

## Example:
```javascript
export const TRANSACTION_TYPES = ['CASH', 'CREDIT'];
export const MAX_RETRY_COUNT = 3;
```
